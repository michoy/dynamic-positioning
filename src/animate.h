#ifndef DATAMETODER_ANIMATE_H
#define DATAMETODER_ANIMATE_H

/**
 * Starts an animation written with freeglut that reads position and thrust from logs/log.dat
 *
 * @param iArgc
 * @param cppArgv
 * @return 0 if the program exits successfully, 1 if it exits with errors.
 */
int animate(int iArgc, char** cppArgv);

#endif //DATAMETODER_ANIMATE_H
